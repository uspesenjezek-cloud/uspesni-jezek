# Prompt za Spark — kopiraj spodnje besedilo

Imam register 133 atomarnih UI-vzorcev v datoteki `NAZORJEVA-VRSTICE.js` (v korenu projekta) — vsak vzorec ima naslov, kratek opis in referenco na obstoječe CSS razrede iz aplikacije (npr. `.uj-card-choices`, `.uj-card-money`, `.uj-card-live`).

Prosim, preoblikuj vizualno predstavitev vseh 133 v ISTEM slogu, kot si ga že uporabil v `NAZORJEVA-PREDLOGI-MOCKUP.html` (72 kartic) — ne izumljaj novega vizualnega jezika, uporabi enak ton, tipografijo, barve in postavitev kot tam.

Pomembno:
- To so **oblike vmesnikov**, ne končna vsebina — vsebuj lahko primer/placeholder podatke (kot v mockupu), a jasno naj bo, da je to PRIMER, ne prava vrednost. Prave podatke in logiko (motoriko) dodam jaz naknadno.
- Vsaka kartica naj ohrani sklic na svojo izvorno vrstico (številko in ID iz registra), da vem, katera predloga je katera, ko jih pozneje povežem s pravimi podatki.
- Izhod naj bo HTML/CSS kartice v enaki obliki kot doslej (samostojne, brez odvisnosti od zunanjih skript) — jaz nato vzamem tvojo obliko in vanjo vežem pravo logiko/podatke.
- Ne spreminjaj pomena vzorca (npr. če je vrstica "denarno polje", naj ostane denarno polje) — spreminjaš samo vizualno izvedbo, ne kaj vzorec predstavlja.

Register (`NAZORJEVA-VRSTICE.js`) najdeš tukaj: `C:\Users\jkjob\Desktop\uspesen jezik git\NAZORJEVA-VRSTICE.js`
Referenčni slog (že narejeno): `http://localhost:8001/NAZORJEVA-PREDLOGI-MOCKUP.html`
